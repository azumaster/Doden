/**
 * 应用页面入口文件 (废弃)
 */
let path = require('path')

module.exports = {
  // 评论
  // comment: path.resolve(__dirname, '../entry/comment/index.js'),
  app: path.resolve(__dirname, '../entry/app.js')
}
